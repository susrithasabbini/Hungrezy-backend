// basic test cases using jest


test('adds 1 + 2 to equal 3', () => {
    expect(1 + 2).toBe(3);
});

test('adds 2 + 2 to equal 4', () => {
    expect(2 + 2).toBe(4);
});

test('adds 3 + 2 to equal 5', () => {
    expect(3 + 2).toBe(5);
});